paths:
  /api/users:
    get:
      summary: Get Users
      description: Get All Users
      tags:
        - Users
      responses:
        200:
          description: Return Users' Array
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Users'
