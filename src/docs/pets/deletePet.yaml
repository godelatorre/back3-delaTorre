paths:
  /api/pets/{pid}:
    delete:
      summary: Delete Pet
      description: Delete a Pet
      tags:
        - Pets
      parameters:
        - in: path
          name: pid
          required: true
          description: Id pet
          schema:
            type: string
      responses:
        200:
          description: Delete Pet
